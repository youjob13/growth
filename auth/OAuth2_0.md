## What is OAuth 2.0?

OAuth 2.0 is an open source **authorization** protocol which allows user (client) get limited access to user data on another service without sharing his login and password.

```md
e.g. login to site via social media account
```
