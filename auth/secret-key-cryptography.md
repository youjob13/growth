## Secret-key cryptography

e.g. Kerberos uses common secret keys.

Every user and every service have unique secret key (only they and KDC know own key). These keys are generated based on user password or special service key.


